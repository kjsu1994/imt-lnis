diff --git a/PocketSDR-AFS/src/sdr_ch.c b/PocketSDR-AFS/src/sdr_ch.c
--- a/PocketSDR-AFS/src/sdr_ch.c
+++ b/PocketSDR-AFS/src/sdr_ch.c
@@ -32,7 +32,9 @@
 #define B_FLL_W    5.0      // band-width of FLL filter (Hz) (wide)
 #define B_FLL_N    2.0      // band-width of FLL filter (Hz) (narrow)
-#define MAX_DOP    5000.0   // max Doppler for acquisition (Hz)
-#define THRES_CN0_L 35.0    // C/N0 threshold (dB-Hz) (lock)
-#define THRES_CN0_U 32.0    // C/N0 threshold (dB-Hz) (lost)
+/* LNIS 변경: 지구 운동 시나리오의 6~8 kHz 도플러도 탐색한다. */
+#define MAX_DOP    15000.0   // max Doppler for acquisition (Hz)
+/* LNIS 변경: 기존 저신호 세기 I/Q 파일도 탐색하되 CRC 통과 관측만 내보낸다. */
+#define THRES_CN0_L 32.0    // C/N0 threshold (dB-Hz) (lock)
+#define THRES_CN0_U 30.0    // C/N0 threshold (dB-Hz) (lost)
 #define THRES_SYNC 0.02     // threshold for sec-code sync
 #define THRES_LOST 0.002    // threshold for sec-code lost
diff --git a/PocketSDR-AFS/src/sdr_nav.c b/PocketSDR-AFS/src/sdr_nav.c
--- a/PocketSDR-AFS/src/sdr_nav.c
+++ b/PocketSDR-AFS/src/sdr_nav.c
@@ -314,7 +314,7 @@
     if (ch->tow <= 0)
     {
-        ch->tow = (int)(tow / 1e-3);
-    }
-    else if (ch->tow == (int)(tow / 1e-3))
+        ch->tow = (int)llround(tow / 1e-3);
+    }
+    else if (ch->tow == (int)llround(tow / 1e-3))
     {
         ch->tow_v = 1; // tow valid
@@ -2603,5 +2603,8 @@
         ch->week = getbitu(data, 0, 13);
         itow = getbitu(data, 13, 9);
-        update_tow(ch, itow * 1200.0 + toi * 12.0 + 0.136); // sync pattern (68 syms) = 68 * 2 ms = 136 ms
+        /* LNIS 변경: CRC 검증된 전체 주차/TOW를 첫 SB2부터 사용한다.
+         * update_tow의 정수 ms 변환은 절삭 대신 반올림해 부동소수점 1ms 오차를 막는다. */
+        update_tow(ch, itow * 1200.0 + toi * 12.0 + 0.136); // sync pattern (68 syms) = 68 * 2 ms = 136 ms
+        if (ch->tow >= 0) ch->tow_v = 1;
         ch->nav->type = 2; // Subframe 2
 
diff --git a/PocketSDR-AFS/src/sdr_rcv.c b/PocketSDR-AFS/src/sdr_rcv.c
--- a/PocketSDR-AFS/src/sdr_rcv.c
+++ b/PocketSDR-AFS/src/sdr_rcv.c
@@ -364,5 +364,6 @@
             }
             // update observation data
-            sdr_pvt_udobs(th->rcv->pvt, th->ix, ch);
+            /* LNIS 변경: 달 PVT/실시간 에포 집계 대신 파일 처리 후 샘플 시각으로
+             * 관측을 합친다. 느린 LDPC 채널을 다른 채널의 집계가 추월하지 않는다. */
             
             // output channel log
@@ -369,4 +370,10 @@
             if (ch->state == SDR_STATE_LOCK && th->ix % LOG_CYC == 0) {
                 out_log_ch(ch);
+                /* LNIS 변경: 의사거리 복원에 필요한 추적 실측치만 출력한다.
+                 * 참조 PVT/송신 RAWX는 이 실행기에 입력하지 않는다. */
+                if (ch->week > 0 && ch->tow_v > 0 && ch->nav->fsync > 0)
+                    sdr_log(3, "$IQOBS,%.3f,%d,%d,%d,%.12f,%.6f,%.6f,%.3f",
+                        ch->time, ch->week, ch->prn, ch->tow,
+                        ch->coff, ch->fd, ch->adr, ch->cn0);
             }
         }
diff --git a/PocketSDR-AFS/src/sdr_pvt.c b/PocketSDR-AFS/src/sdr_pvt.c
--- a/PocketSDR-AFS/src/sdr_pvt.c
+++ b/PocketSDR-AFS/src/sdr_pvt.c
@@ -482,5 +482,5 @@
     }
     else if (!strcmp(ch->sig, "AFSD")) { // TEB: AFSD NAV
-        if (ch->nav->type == 2 && decode_afs_frame(data, ch->prn)) {
+        if (false /* LNIS 변경: 달 항법 배열(12 PRN)에 지구 GPS PRN을 넣지 않는다. */) {
 
             pvt->count[2]++;
@@ -627,5 +627,5 @@
         if (pvt_afs) {
             // TEB: Update AFS PVT solutions
-            update_sol_afs(pvt);
+            /* LNIS 변경: 달 측위 비활성. 별도 RTKLIB 지구 PVT 연결부를 사용한다. */
         }
         else
diff --git a/PocketSDR-AFS/lib/RTKLIB/src/rtklib.h b/PocketSDR-AFS/lib/RTKLIB/src/rtklib.h
--- a/PocketSDR-AFS/lib/RTKLIB/src/rtklib.h
+++ b/PocketSDR-AFS/lib/RTKLIB/src/rtklib.h
@@ -216,5 +216,10 @@
 #ifdef ENALEO
 #define MINPRNLEO   1                   /* min satellite sat number of LEO */
-#define MAXPRNLEO   12                  /* max satellite sat number of LEO */ /* TEB: 2025-04-25 Set max PRN to 12 for LANS AFS */
+#ifdef LNIS_IQ_RECEIVER
+/* LNIS 변경: AFS 변조 채널에 GPS PRN 1~32를 매핑한다. 기존 코덱 빌드는 유지한다. */
+#define MAXPRNLEO   32
+#else
+#define MAXPRNLEO   12
+#endif                  /* max satellite sat number of LEO */ /* TEB: 2025-04-25 Set max PRN to 12 for LANS AFS */
 #define NSATLEO     (MAXPRNLEO-MINPRNLEO+1) /* number of LEO satellites */
 #define NSYSLEO     1
diff --git a/PocketSDR-AFS/src/sdr_func.c b/PocketSDR-AFS/src/sdr_func.c
--- a/PocketSDR-AFS/src/sdr_func.c
+++ b/PocketSDR-AFS/src/sdr_func.c
@@ -748,1 +748,2 @@
-    int stat = 0, port = 0, str_opt[] = {30000, 30000, 1000, 1<<20, 0};
+    /* LNIS 변경: RTKLIB strsetopt는 예약 필드까지 8개 항목을 읽는다. */
+    int stat = 0, port = 0, str_opt[] = {30000, 30000, 1000, 1<<20, 0, 0, 0, 0};
@@ -834,1 +835,2 @@
-        if (log_buff_p + len + 1 < MAX_LOG_BUFF) {
+        /* LNIS 변경: CR/LF와 추가 LF, NUL까지 버퍼 경계에 포함한다. */
+        if (log_buff_p + len + 4 <= MAX_LOG_BUFF) {
diff --git a/LANS-AFS-SIM/afs_sim.c b/LANS-AFS-SIM/afs_sim.c
--- a/LANS-AFS-SIM/afs_sim.c
+++ b/LANS-AFS-SIM/afs_sim.c
@@ -1385,5 +1385,7 @@
 
             // Path loss
-            path_loss = 5200000.0 / rho.range;
+            /* LNIS 변경: 달의 5,200 km 대신 GPS의 20,200 km 기준 신호 세기.
+             * 원본 거리 반비례/잡음/변조 방식은 유지하며 지구 링크의 과도한 감쇠를 방지한다. */
+            path_loss = 20200000.0 / rho.range;
 
             // Gain
